server:
  port: 8080
  servlet:
    session:
      cookie:
        name: SESSIONDEMO

#    mysql 数据库配置
spring:
  resources:
    static-locations=classpath:/
  datasource:
    url: jdbc:mysql://localhost:3306/zhujianju?serverTimezone=Hongkong&useSSL=false&useUnicode=true&characterEncoding=utf8&allowMultiQueries=true&allowPublicKeyRetrieval=true


    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: aaaa1111!

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true


